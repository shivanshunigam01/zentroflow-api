NODE_ENV=development
PORT=8787
RAZORPAY_API_PORT=8787

API_PREFIX=/api/v1
# Comma-separated frontend origins (JWT via Authorization — credentials: false)
# Do NOT set CORS headers again in nginx (duplicate ACAO → browser Failed to fetch)
CORS_ORIGIN=https://zentroverse-automation.vercel.app,https://flow.zentroverse.com,http://localhost:5173
JWT_SECRET=change-me-in-production
DUPLICATE_WINDOW_DAYS=30

# MongoDB Atlas (replace <db_password>)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.example.mongodb.net/zentroverse?authSource=admin&retryWrites=true&w=majority&appName=Cluster0
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_HOST=cluster0.example.mongodb.net
MONGODB_DB=zentroverse

# Razorpay (server only)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Surepass
SUREPASS_BASE_URL=https://kyc-api.surepass.io
SUREPASS_TOKEN=

# Legacy env admin (still works for login)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-in-production

# Auto-created on first start if no users in DB
DEFAULT_USER_EMAIL=buddy@zentroverse.com
DEFAULT_USER_PASSWORD=Zentroflow@2026

# AiSensy Partner API
AISENSY_PARTNER_ID=
AISENSY_PARTNER_API_KEY=
AISENSY_SHARED_SECRET=
AISENSY_PARTNER_PLAN_FAMILY_ID=
AISENSY_PLAN_FAMILY_ID=

# WhatsApp bulk campaign (Lead Inbox — never expose apiKey to frontend)
WHATSAPP_CAMPAIGN_API_URL=https://backend.api-wa.co/campaign/zentroverse-global/api/v2
WHATSAPP_CAMPAIGN_API_KEY=
WHATSAPP_CAMPAIGN_NAME=flowtest
WHATSAPP_CAMPAIGN_USER_NAME=Zentroverse
WHATSAPP_CAMPAIGN_SOURCE=zentroflow-lead-inbox
WHATSAPP_CAMPAIGN_DELAY_MS=350

# Campaign report (connect.api-wa.co) — uses AISENSY_PARTNER_API_KEY + project/campaign ids
# WHATSAPP_PROJECT_ID=   # optional; auto-read from JWT id in WHATSAPP_CAMPAIGN_API_KEY
# WHATSAPP_CAMPAIGN_ID=  # optional; use when campaign-details API key is expired — report still works with partner key

# Max leads loaded on GET /bootstrap (inbox sync). Default 10000.
BOOTSTRAP_MAX_LEADS=10000

# Tata Smartflo — backend only (never expose SMARTFLO_API_TOKEN to the frontend)
SMARTFLO_API_TOKEN=
SMARTFLO_LEAD_LIST_ID=
SMARTFLO_BASE_URL=https://api-smartflo.tatateleservices.com
SMARTFLO_API_BASE_URL=https://api-smartflo.tatateleservices.com/v1
SMARTFLO_CAMPAIGN_ID=
SMARTFLO_DISPOSITION_LIST_ID=
# dial_out_each_call (production) | session
SMARTFLO_DIALER_MODE=dial_out_each_call
# Optional shared secret for POST /api/v1/webhooks/smartflo/dialer
SMARTFLO_WEBHOOK_SECRET=

# Click-to-Call Support API (API Connect → Click to Call Support API)
SMARTFLO_CLICK_TO_CALL_API_KEY=
SMARTFLO_IVR_ID=83389
SMARTFLO_CLICK_TO_CALL_ENDPOINT=/v1/click_to_call_support
# Optional DID for outbound caller ID — format 9180694XXXXX (required for direct agent call)
# SMARTFLO_CALLER_ID=

# Normal / agent Click-to-Call Support API key (API Connect — agent flow)
# SMARTFLO_AGENT_CALL_API_KEY=

# Direct agent Click-to-Call — POST /v1/click_to_call (uses SMARTFLO_API_TOKEN Bearer auth)
SMARTFLO_DIRECT_CALL_ENDPOINT=/v1/click_to_call
# Smartflo agent extension / ID (e.g. 101 or agent mobile registered in panel)
SMARTFLO_AGENT_NUMBER=
